/* Self-hosted IBM Plex (bundled by Vite, so no Google Fonts CDN dependency —
   works where fonts.googleapis.com is blocked, and rides VITE_ASSET_BASE).
   Only the latin woff2 weights we use are shipped; see styles/fonts.css. */
import "./styles/fonts.css";
